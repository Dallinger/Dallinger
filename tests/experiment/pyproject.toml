[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "test-experiment"
version = "0.1.0"
description = "Test experiment for Dallinger"
requires-python = ">=3.10"
dependencies = [
    "dallinger",
]

[project.optional-dependencies]
dev = [
    "pytest",
]
